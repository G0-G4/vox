## описание

Реализовать утилиту командной строки для управления сценаринкаем звонка.

## Необходимые команды

```bash
# 📦 Публикация и управление версиями
vox publish <file>                          # Публикация с авто-бэкапом
vox publish <file> --no-backup              # Публикация без бэкапа

# 🔄 Управление версиями
vox rollback                                # Откат на последний бэкап
vox rollback --target <backup-name>         # Откат на конкретный бэкап
vox rollback --list                         # Показать доступные для отката версии

# 💾 Управление бэкапами
vox backup --list                           # Список всех бэкапов
vox backup --create [name]                  # Создать бэкап вручную

# инициализация

vox init                                   # вызывает npx voxengine-ci init
vox init --force                           # синхронизация с платформой вызывает npx voxengine-ci init --force

# По умолчанию берется конфиг из папки с утилитой .vox-config.json но должна быть возможность запустить с другим
vox --config <config> publish|backup...

# ℹ️ Информация
vox --version                               # Версия CLI
vox --help                                  # Справка
```

## Бэкапы

При публикации текущий сценарий должен копироваться в сценарий с тем же названием с постфиксом времени обновления. Например при публикации сценария main.js текущий должен быть скопирован в main_backup_2025-01-01_12-30-12.js

При создании бэкапа по имени должен создасться бэкап с названием main_backup_name.js. Если такой файл уже есть необходимо вывести сообщение об ошибке.

При publish и rollback выводить, какое действие будет совершено и просить подтвердить действие просьбой ввести "publish"

- команды init + параметр --config
- реализация publish
- добавить бэкап при публикации
- команды backup --list и backup --create
- команды rollback